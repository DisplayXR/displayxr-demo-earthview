# Build output
build/
build-*/
spike/build/

# Third-party clones (pinned by tag in spike/CMakeLists.txt, not vendored)
third_party/cesium-native/

# API keys / secrets — never commit
earthview.ini
*.key
.env
.env.local
.env.*

# Editor / OS
.DS_Store
.idea/
.vscode/
compile_commands.json

# Claude Code worktrees
.claude/worktrees/

# M0 render artifact (Google imagery — not committed; regenerate via spike)
docs/m0_paris.png
docs_m0_paris.png

# orbit-mode render artifacts (Google imagery)
/tmp earthview_orbit
earthview_orbit_*.png

# clangd index cache
.cache/

# Build / installer staging outputs
_artifact/
_package/

# Built installers (built by CI / build-installer.bat)
installer/**/*.exe

# Android leg
android/build/
android/.cxx/
.gradle/
local.properties
android/local.properties
*.apk
